export enum ServerErrors{
  notFound,
  emptyFields,
  loginRepeat,
  passwordIsNotCorrect
}

export enum GameStage{
  connecting,
  preparingForGame,
  fillingInField
}

export enum Status{
  connected,
  disconnected
}