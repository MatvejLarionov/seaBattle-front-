export enum ServerErrors{
  notFound,
  emptyFields,
  loginRepeat,
  passwordIsNotCorrect
}

export enum GameStage{
  connecting,
  preparingForGame
}

export enum Status{
  connected,
  disconnected
}